import React from "react";

interface NavbarMenuPortfolioProps {
  content: string;
  text: string;
  children: React.ReactNode;
}

const NavbarMenuPortfolio: React.FC<NavbarMenuPortfolioProps> = ({}) => {
  return <div></div>;
};

export default NavbarMenuPortfolio;
 